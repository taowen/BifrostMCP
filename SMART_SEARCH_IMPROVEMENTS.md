# 智能搜索项目结构集成改进

## 改进概述

在 `analyzeQueryAndSearch` 函数中集成了项目结构信息，让大模型能够基于当前项目的实际结构来提供更精准的搜索策略。

## 主要改进

### 1. 新增项目结构获取功能 (utils.ts)

添加了 `getProjectStructure()` 函数，包含以下功能：

- **目录树构建**: 递归构建项目文件树，自动过滤常见的无关目录（如 node_modules、.git 等）
- **配置文件检测**: 自动识别重要配置文件（package.json、tsconfig.json、webpack.config.js 等）
- **package.json 解析**: 提取项目名称、版本、描述、入口点和脚本信息
- **智能限制**: 限制目录深度和显示条目数，避免信息过载

### 2. 增强的搜索分析 (intent.ts)

#### `analyzeQueryAndSearch` 函数改进：
- 在调用大模型前先获取项目结构信息
- 将项目结构作为上下文提供给大模型
- 指导大模型根据项目类型和结构制定精准搜索策略
- 提供更详细的搜索指导原则

#### `analyzeIntentAndExtractEntities` 函数改进：
- 同样集成了项目结构信息
- 增强了意图分析的准确性

## 技术实现细节

### 目录树构建算法
```typescript
async function buildDirectoryTree(uri: vscode.Uri, prefix: string, depth: number, maxDepth: number)
```
- 使用递归算法构建目录树
- 智能过滤不重要的目录和文件
- 限制深度（默认3层）和条目数（每层最多20个）
- 提供清晰的树形结构显示

### 配置文件分析
- 支持多种常见配置文件类型
- 对 package.json 进行深度解析
- 提取项目关键信息用于搜索优化

### 上下文集成
- 将项目结构无缝集成到大模型提示中
- 提供结构化的项目信息
- 指导大模型基于实际项目状况制定搜索策略

## 预期效果

1. **更精准的搜索**: 大模型能够了解项目实际结构，提供更相关的搜索建议
2. **项目感知**: 根据项目类型（TypeScript、JavaScript、配置等）调整搜索策略
3. **入口点识别**: 自动识别项目入口点和重要文件
4. **智能文件过滤**: 根据项目结构选择合适的文件模式和搜索范围

## 使用场景

- 用户查询项目入口点时，能自动关联 package.json 中的 main 字段
- 查询特定功能时，能根据目录结构推断最可能的位置
- 搜索配置相关内容时，能优先检查相关配置文件
- 理解项目架构时，提供完整的结构概览

## 代码质量

- 遵循 TypeScript 严格模式
- 包含完整的错误处理
- 添加了详细的 JSDoc 注释
- 符合项目现有的编码规范
